import React from 'react';
import { Redirect } from '@docusaurus/router';

export default function RedirectPage() {
  // redirect plugin threw broken links during build
  return <Redirect to="/data/security" />;
}